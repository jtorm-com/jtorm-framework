# jTorm ViewModel

Creates and copies the render view object. Method control flow is not stored on the view; verbs
return effects to `@jtorm/handler` instead.

## Install
```js
npm install @jtorm/tss-model
```
