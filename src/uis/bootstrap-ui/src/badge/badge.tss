/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(to: 'array') {
    ->else
    ->if(to: 'object')
    ->data(value: label || count) {
        .jtorm-badge:last-child->attr {
            n: 'class';
            v: 'text-bg-secondary';
            m: 'a';
        }
    }
}
