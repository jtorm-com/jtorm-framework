/*! (c) jTorm and other contributors | www.jtorm.com/license */

style {
    ->attr {
        n: 'media';
        v: media;
    }

    // raw-text element — content (CSS) serializes verbatim; raw slot, not replacable.
    ->get {
        t: '@h/rawcontent.tss';
    }
}
