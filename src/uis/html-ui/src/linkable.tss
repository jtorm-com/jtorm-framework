/*! (c) jTorm and other contributors | www.jtorm.com/license */
->attr {
    n: 'href';
    v: href;
}

->if(d: hreflang, v: '^([a-z][a-z]-?([A-Z][A-Z]|Hans|Hant|x-default)?)$')->attr {
    n: 'hreflang';
    v: hreflang;
}

->attr {
    n: 'media';
    v: media;
}

->attr {
    n: 'type';
    v: type;
}

->get {
    t: '@h/global.tss';
}