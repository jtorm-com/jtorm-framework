/*! (c) jTorm and other contributors | www.jtorm.com/license */

input {
    ->attr {
        n: 'type';
        v: 'date';
    }

    ->attr {
        n: 'pattern';
        v: pattern;
    }

    ->get {
        t: '@h/form/input-range-attrs.tss';
    }
}
