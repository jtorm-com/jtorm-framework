/*! (c) jTorm and other contributors | www.jtorm.com/license */
output {
  ->if {
    d: for;
    ->attr {
      n: 'for';
      v: for;
    }
  }
  ->if {
    d: form;
    ->attr {
      n: 'form';
      v: form;
    }
  }
  ->if {
    d: name;
    ->attr {
      n: 'name';
      v: name;
    }
  }
  ->get {
    t: '@h/replacable.tss';
  }
}