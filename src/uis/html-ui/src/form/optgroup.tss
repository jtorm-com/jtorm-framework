/*! (c) jTorm and other contributors | www.jtorm.com/license */

optgroup {
    ->if(d: disabled)->attr {
        n: 'disabled';
        v: true;
    }

    ->attr {
        n: 'label';
        v: label;
    }

    ->get {
        t: '@h/replacable.tss';
    }
}
