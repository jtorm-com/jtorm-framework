{
  "html": {
    "@description": "Text content, HTML-escaped by default (rendered inert). For author-trusted raw HTML/markup use the insert h: param."
  },
  "type": {
    "@description": "Specifies the type",
    "@options": [
      {"value": "button", "description": ""},
      {"value": "reset", "description": ""},
      {"value": "submit", "description": ""}
    ]
  }
}