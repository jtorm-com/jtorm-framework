/*! (c) jTorm and other contributors | www.jtorm.com/license */

button {
    ->inner {
        t: html;
    }

    ->if(d: type, v: '^(button|reset|submit)$', r: true)
    ->attr {
        n: 'type';
        v: type;
    }

    ->get {
        t: '@h/form/form-element.tss';
    }
}
