/*! (c) jTorm and other contributors | www.jtorm.com/license */
input {
    ->attr {
        n: 'type';
        v: 'range';
    }

    ->if(d: autocomplete, v: '^(on|off)$')->attr {
        n: 'autocomplete';
        v: autocomplete;
    }

    ->get {
        t: '@h/form/input-range-attrs.tss';
    }
}