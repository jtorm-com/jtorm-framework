/*! (c) jTorm and other contributors | www.jtorm.com/license */
input {
  ->attr {
    n: 'type';
    v: 'password';
  }
  ->attr {
    n: 'autocomplete';
    v: 'off';
  }
  ->if {
    d: pattern;
    ->attr {
      n: 'pattern';
      v: pattern;
    }
  }
  ->get {
    t: '@h/form/input.tss';
  }
}