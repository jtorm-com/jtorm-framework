/*! (c) jTorm and other contributors | www.jtorm.com/license */

bdo {
    ->if(d: dir, v: '^(ltr|rtl)$', r: true)
    ->attr {
        n: 'dir';
        v: dir;
    }

    ->get {
        t: '@h/replacable.tss';
    }
}