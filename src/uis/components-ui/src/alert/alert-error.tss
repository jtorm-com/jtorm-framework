/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(to: 'array') {
    ->else
    ->if(to: 'object')
    ->data(intent: 'error', role: 'alert')
    ->get {
        t: '@c/alert/alert-base.tss';
    }
}
