/*! (c) jTorm and other contributors | www.jtorm.com/license */

->each {
    m: 'append';
    d: @config.appleTouchIcon;

    ->append->ui {
        c: 'doc.link';

        link->attr {
            n: 'rel';
            v: 'apple-touch-icon';
        }
    }
}

->append(d: @config.favicon)->ui {
    c: 'doc.link';

    link->attr {
        n: 'rel';
        v: 'icon';
    }
}

->data(charset: @meta.charset||'UTF-8')->append->ui {
    c: 'doc.meta';
}

->if(d: rating)->data(name: 'rating', content: rating)->append->ui {
    c: 'doc.meta';
}

->data(httpEquiv: 'X-UA-Compatible', content: @meta.XUACompatible||'IE=Edge')->append->ui {
    c: 'doc.meta';
}

->data(name: 'viewport', content: @meta.viewport||'width=device-width, initial-scale=1')->append->ui {
    c: 'doc.meta';
}

->append(d: @config.facebook)->get {
    t: '@c/facebook.tss';
}

->append(d: @config.twitter)->get {
    t: '@c/twitter.tss';
}

->append(d: @config.google)->get {
    t: '@c/google.tss';
}

->append(d: @config.og)->get {
    t: '@c/og.tss';
}
