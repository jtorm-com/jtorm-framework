/*! (c) jTorm and other contributors | www.jtorm.com/license */
// hoe dit te cachen? layer?

// identifier
->title {
    t: name.Text;
}
html->attr {
    n: 'lang';
    v: mainEntity.@c.inLanguage.Text;
}
/*
head {
    // identifier
    ->data(name: 'description', content: disambiguatingDescription.Text)->append->ui {
        c: 'doc.meta';
    }
    ->data(rel: 'canonical', href: url.URL)->append->ui {
        c: 'doc.link';
    }
    ->data(h: url.URL)->append->ui {
        c: 'doc.link';
        t: 0;
        link {
            ->attrs {
                n: 'rel,type';
                v: 'alternate,application/ld+json';
            }
            ->attr {
                n: 'href';
                v: h;
                a: '.jsonld';
            }
        }
    }
    ->if(d: workTranslation)->each {
        m: 'append';
        d: workTranslation;
        ->ui {
            c: 'doc.link';
            link {
                ->attrs {
                    n: 'rel,hreflang,href';
                    v: 'alternate,@c.inLanguage.Language.alternateName.Text,@c.url.URL';
                }
            }
        }
    }
    ->data(name: 'robots', content: @meta.robots||'index, follow')->append->ui {
        c: 'doc.meta';
    }
    ->data(name: 'referrer', content: @meta.referrer||'origin')->append->ui {
        c: 'doc.meta';
    }

    // global
    ->each {
        m: 'append';
        d: @config.appleTouchIcon;
        ->append->ui {
            c: 'doc.link';
            link->attr {
                n: 'rel';
                v: 'apple-touch-icon';
            }
        }
    }
    ->append(d: @config.favicon)->ui {
        c: 'doc.link';
        link->attrs {
            n: 'rel';
            v: 'icon';
        }
    }
    ->data(charset: @meta.charset||'UTF-8')->append->ui {
        c: 'doc.meta';
    }
    ->data(httpEquiv: 'X-UA-Compatible', content: @meta.XUACompatible||'IE=Edge')->append->ui {
        c: 'doc.meta';
    }
    ->data(name: 'viewport', content: @meta.viewport||'width=device-width, initial-scale=1')->append->ui {
        c: 'doc.meta';
    }
}
*/
/* todo
  ->if(d: twitter)->get {
    t: '@c/twitter.tss';
  }
  ->if(d: fb)->get {
    t: '@c/facebook.tss';
    ->if(d: og)->get {
      t: '@c/og.tss';
    }
  }
*/
->if(d: @config.google)->get {
  t: '@c/google.tss';
}
