/*! (c) jTorm and other contributors | www.jtorm.com/license */

section {
    ->attrs {
        n: 'id,class,style,data-nosnippet';
        v: 'loading,loading,display: none,1';
        m: 'p';
    }

    .loading->append->ui {
        c: '@e.div';
        t: '0';

        div {
            ->append->ui {
                c: '@e.span';
                t: '0';
            }

            ->append->ui {
                c: '@t.small';
                t: '0';

                small->text(label: 'Loading')->inner {
                    h: label;
                }
            }
        }
    }
}
