/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(to: 'array') {
    ->else
    ->if(to: 'object')
    ->data(intent: 'destructive')
    ->get {
        t: '@c/button/button-base.tss';
    }
}
