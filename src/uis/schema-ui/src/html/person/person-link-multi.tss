/*! (c) jTorm and other contributors | www.jtorm.com/license */
->append(d: Person)->ui {
  c: 'Person.link';
}
->if(to: 'array')->each {
  m: 'append';
  body->append(d: Person)->ui {
    c: 'Person.link';
  }
}