/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(d: url)->append->ui {
    c: 'Thing.link';
}

->if(to: 'array')->each {
    m: 'append';

    body->append->ui {
        c: @type + '.link';
    }
}
