/*! (c) jTorm and other contributors | www.jtorm.com/license */

->append(d: Action)->ui {
    c: 'Action.link';
}

->if(to: 'array')->append->ui {
    c: '@e.div';
    t: '0';

    div {
        ->attr {
            n: 'class';
            v: 'schema-Action-group';
        }

        .schema-Action-group->each {
            m: 'append';

            body->append(d: @c)->ui {
                c: @type + '.link';
            }
        }
    }
}
