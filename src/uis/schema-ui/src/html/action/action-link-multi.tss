/*! (c) jTorm and other contributors | www.jtorm.com/license */
->append(d: Action)->ui {
  c: 'Thing.link';
}
->if(to: 'array')->each {
  m: 'append';
  body->append(d: Action)->ui {
    c: 'Thing.link';
  }
}