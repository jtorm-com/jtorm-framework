/*! (c) jTorm and other contributors | www.jtorm.com/license */
->append(d: CreativeWork)->ui {
  c: 'CreativeWork.link';
}
->if(to: 'array')->each {
  m: 'append';
  body->append(d: CreativeWork)->ui {
    c: 'CreativeWork.link';
  }
}