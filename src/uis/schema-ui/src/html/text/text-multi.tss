/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(d: Text)->ui {
    c: 'Text';
}

->if(to: 'array')->each {
    m: 'append';

    body->if(d: Text)->ui {
        c: 'Text';
    }
}