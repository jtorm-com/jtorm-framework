/*! (c) jTorm and other contributors | www.jtorm.com/license */
->append(d: Event)->ui {
  c: 'Event.link';
}
->if(to: 'array')->each {
  m: 'append';
  body->append(d: Event)->ui {
    c: 'Event.link';
  }
}