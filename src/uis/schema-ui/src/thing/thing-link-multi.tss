/*! (c) jTorm and other contributors | www.jtorm.com/license */

->if(d: url)->append->ui {
    c: 'Thing.link';
}

->if(to: 'array')->ui {
    c: '@e.div';
    t: '0';

    div {
        ->attr {
            n: 'class';
            v: 'schema-Thing-group';
        }

        ->each {
            m: 'append';

            body->append->ui {
                c: @type + '.link';
            }
        }
    }
}