/*! (c) jTorm and other contributors | www.jtorm.com/license */

->each(d: action, m: 'append', a: 'action')
->ui {
    c: '@e.div';
    t: '0';

    div {
        ->attr {
            n: 'class';
            v: 'schema-Action';
        }

        ->append(d: target)->ui {
            c: 'EntryPoint';
        }
    }
}
