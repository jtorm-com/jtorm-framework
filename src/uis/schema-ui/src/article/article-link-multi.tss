/*! (c) jTorm and other contributors | www.jtorm.com/license */
->append(d: Article)->ui {
  c: 'Article.link';
}
->if(to: 'array')->each {
  m: 'append';
  body->append(d: Article)->ui {
    c: 'Article.link';
  }
}