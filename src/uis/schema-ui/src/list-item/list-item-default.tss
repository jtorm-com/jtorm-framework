/*! (c) jTorm and other contributors | www.jtorm.com/license */

->ui {
    c: '@t.li';
    t: '0';

    li {
        ->attr {
            n: 'value';
            v: position;
        }

        ->if(d: item) {
            ->append(d: item)
            ->ui {
                c: @type + '.item';
            }

            ->else
            ->if(
                d: @type,
                v: '^(?!ListItem$).+'
            )
            ->append
            ->ui {
                c: @type + '.item';
            }
        }
    }
}
